query getAll {
  assemblies {
    edges {
      node {
        id: ID!
        name: String!
        description: String
        productFamilies: [String]
        category: String
      }
    }
  }
}
